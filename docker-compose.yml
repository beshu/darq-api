version: '3'

services:
  db:
    image: mongo:latest
  web_app:
    build:
      dockerfile: Dockerfile
      context: .
    image: darqube-api
    depends_on:
      - db
    ports:
      - "5000:5000"